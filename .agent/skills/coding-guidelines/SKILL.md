---
name: coding-guidelines
description: AI 编码行为准则，减少过度工程化、乱改代码等常见 LLM 错误。在开始任何编码任务之前都应加载此 skill——无论是新功能、bug修复、重构、调试还是代码审查。如果用户说"帮我写"、"修这个bug"、"优化一下"、"加个功能"，这个 skill 都是适用的。
---

# 编码行为准则（Karpathy Guidelines）

减少常见 LLM 编码错误的行为指南，来源于 Andrej Karpathy 对 LLM 编码陷阱的观察。

> **权衡说明**：这些指南偏向谨慎而非速度。对于琐碎任务，灵活使用判断力。

---

## 1. 编码前先思考

**不要假设。不要隐藏困惑。暴露权衡。**

开始实现前：
- 明确陈述你的假设。如果不确定，先询问。
- 如果存在多种解读，列出它们——不要静默地做选择。
- 如果有更简单的方案，说出来。必要时推回需求。
- 如果有不清楚的地方，停下来。指出什么让你困惑。然后问。

---

## 2. 简洁优先

**最少的代码解决问题。不做推测性扩展。**

- 不添加未被要求的功能。
- 不为单次使用的代码做抽象。
- 不添加未被请求的"灵活性"或"可配置性"。
- 不为不可能发生的场景添加错误处理。
- 如果你写了 200 行但 50 行就够，重写它。

**自检问题**：「一位资深工程师会说这过于复杂吗？」如果是，就简化。

---

## 3. 外科式修改

**只改动必须改动的。只清理你自己制造的混乱。**

编辑现有代码时：
- 不要"顺手改进"相邻代码、注释或格式。
- 不要重构没有坏掉的东西。
- 匹配现有风格，即使你会用不同的方式写。
- 如果你发现不相关的死代码，提及它——不要删除它。

当你的改动产生孤儿：
- 删除**你的改动**导致的无用 import/变量/函数。
- 不删除已有的死代码（除非被要求）。

**检验标准**：每一行被修改的代码都应该能直接追溯到用户的请求。

---

## 4. 目标驱动执行

**定义成功标准。循环直到验证通过。**

将任务转化为可验证的目标：
- "添加验证" → "为无效输入编写测试，然后让测试通过"
- "修复这个 bug" → "编写复现测试，然后让测试通过"
- "重构 X" → "确保重构前后测试都通过"

多步任务，先陈述简短计划：
```
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
3. [步骤] → 验证：[检查项]
```

强成功标准让你可以独立循环完成任务。弱标准（"让它工作"）需要反复确认。

---

## 在本项目中的应用

| 场景 | 应用准则 |
|------|---------|
| 创建新页面 | 用 uni-page-generator skill；只实现当前需求的页面结构 |
| 修改 API 接口 | 只改 `src/api/` 对应文件；不顺带重构 HTTP 层 |
| 样式调整 | 优先原子化类名；不重写整个组件样式 |
| TypeScript 报错 | 修复报错本身；不重构周边类型定义 |
| 性能优化 | 先确认问题存在；不做未经验证的推测性优化 |
