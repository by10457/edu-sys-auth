{
  "name": "edu-sys-auth",
  "private": true,
  "description": "Hello Egg.js",
  "homepage": "https://github.com/YOUR_USERNAME/YOUR_REPO#readme",
  "bugs": {
    "url": "https://github.com/YOUR_USERNAME/YOUR_REPO/issues"
  },
  "license": "MIT",
  "author": "Author Name <author.name@mail.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YOUR_USERNAME/YOUR_REPO.git"
  },
  "type": "module",
  "scripts": {
    "start": "eggctl start --daemon --title=egg-server-edu-sys-auth",
    "stop": "eggctl stop --title=egg-server-edu-sys-auth",
    "predev": "npm run clean",
    "dev": "egg-bin dev",
    "worker:dev": "node --import @oxc-node/core/register app/worker/loginWorker.ts",
    "test:local": "cross-env NODE_OPTIONS=\"--import @oxc-node/core/register\" vitest run",
    "pretest": "npm run clean && npm run lint -- --fix",
    "test": "npm run test:local",
    "preci": "npm run clean && npm run lint",
    "ci": "npm run test:local -- --coverage",
    "postci": "npm run prepublishOnly && npm start && sleep 10 && npm stop && npm run clean",
    "lint": "oxlint --type-aware",
    "typecheck": "tsgo --noEmit",
    "build": "tsc",
    "clean": "tsc -b --clean",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "dependencies": {
    "@eggjs/scripts": "beta",
    "@eggjs/tracer": "beta",
    "bullmq": "^5.74.1",
    "egg": "beta",
    "egg-redis": "^2.6.1",
    "ioredis": "^5.10.1",
    "playwright": "^1.59.1"
  },
  "devDependencies": {
    "@eggjs/bin": "beta",
    "@eggjs/mock": "beta",
    "@eggjs/tsconfig": "beta",
    "@oxc-node/core": "^0.0.35",
    "@types/node": "24",
    "@vitest/coverage-v8": "4",
    "cross-env": "10",
    "oxlint": "1",
    "oxlint-tsgolint": "^0.21.0",
    "prettier": "^3.8.3",
    "typescript": "5",
    "vitest": "4"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "packageManager": "npm@11.6.0"
}
